<%- include('../includes/head.ejs') %>
<link rel="stylesheet" href="../../product-details.css" />
</head>

<body>
  <%- include('../includes/nav.ejs') %>

<div class="product__grid">
  <div class="product__main">
    <img src="<%= prod.imageUrl %>" alt="<%= prod.name %>">
  </div>
  <div class="product__secondary">
    <div class="product__info">
      <div class="product__header">
        <h3>Product Details</h3>
        <h2><%= prod.name %></h2>
        <p class="product__price">$<%= prod.price %></p>
      </div>
      <div class="product__details">
        <p class="product__description"><%= prod.description %></p>
      </div>
      <div class="product__actions">
        <form action="/cart" method="POST">
          <input type="hidden" name="productId" value="<%= prod.id %>">
          <button class="btn" type="submit">ADD TO CART</button>
        </form>
      </div>
      <div class="product__additional">
        <p class="product__highlight">PAY OVER TIME IN INTEREST-FREE INSTALLMENTS WITH AFFIRM, KLARNA OR AFTERPAY</p>
        <p class="product__highlight">JOIN ADICLUB TO GET UNLIMITED FREE STANDARD SHIPPING, RETURNS, & EXCHANGES</p>
      </div>

    </div>
  </div>
</div>

  <%- include('../includes/end.ejs') %>